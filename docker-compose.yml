services:
  # Base compose file (structure-only).
  # Run:
  #   docker compose -f docker-compose.dev.yml up
  #   docker compose -f docker-compose.prod.yml up
  backend:
    image: placeholder
    networks: [app-network]

  frontend:
    image: placeholder
    networks: [app-network]

networks:
  app-network:
    driver: bridge



